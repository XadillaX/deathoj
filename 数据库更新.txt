$Id$
config表中：
增加ojname为NBUTOJ。

contestuser表中：
添加一个字段teamname VARCHAR(32) DEFAULT("") unique key

$Id$
problem表中
增加字段inputmd5(VARCHAR(32)) outputmd5(VARCHAR(32)) input(TEXT) output(TEXT)

$Id$
problem表中
problemid的AUTO_INCREMENT改为1000

$Id$
contest表中
增加字段resultversion(varchar(14), default(00000000000000))